For vimrc, rename to .vimrc and place in ~/

For vscode, copy the settings and place in settings.json in vscode

for vscode extensions, your extensions folder is located on one of the following paths:

Windows: %USERPROFILE%\.vscode\extensions
Mac: ~/.vscode/extensions
Linux: ~/.vscode/extensions
